# Python
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# Node
node_modules/
*.tsbuildinfo

# Expo / React Native
app/.expo/
app/dist/
app/ios/
app/android/
*.jks
*.p8
*.p12
*.mobileprovision

# Training checkpoints and datasets are large and regenerable. The exported ONNX
# classifier is ~35 KB and is committed, so a clean checkout deploys without
# needing to train first.
models/*.pt
models/*.ckpt
data/

# Local config and secrets
.env
.env.*
!.env.example

# OS
.DS_Store
